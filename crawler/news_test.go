package crawler

import "testing"

func TestNews(t *testing.T) {
	tests := []struct {
		name string
	}{
		{"tttt"},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// News()
		})
	}
}
